    -- pour les connection 
az login
    -- pour la connecxion avec cluster
az aks get-credentials --resource-group RG_PROMO --name cluster_promo
    -- visualiser les nodes sur "cluster_promo"
kubectl get nodes

    -- pour crées le namespace a partir du Yaml 
kubectl apply -f Namespace.yaml


Voici l’ordre logique et les commandes à utiliser pour appliquer chaque fichier YAML, un par un, dans ton namespace `jimpe`.  
Garde cette structure : **crée, puis “apply” chaque fichier** dans cet ordre recommandé.

---

### **1. Namespace** *(Déjà fait)*
```powershell
kubectl apply -f Namespace.yaml
```

---

### **2. Secret MySQL**
```powershell
kubectl apply -f Secret-MySQL.yaml -n jimpe
```

---

### **3. Persistent Volume Claim (PVC) MySQL**
```powershell
kubectl apply -f PVC-MySQL.yaml -n jimpe
```

---

### **4. Déploiement MySQL**
```powershell
kubectl apply -f Deployment-MySQL.yaml -n jimpe
```

---

### **5. Service MySQL**
```powershell
kubectl apply -f Service-MySQL.yaml -n jimpe
```

---

### **6. Déploiement API Python**
```powershell
kubectl apply -f Deployment-API.yaml -n jimpe
```

---

### **7. Service API**
```powershell
kubectl apply -f Service-API.yaml -n jimpe
```

---

### **8. Ingress (routes externes/AKS)**
```powershell
kubectl apply -f Ingress.yaml -n jimpe
```

---

### **Résumé de l’ordre à respecter**
1. Namespace  
2. Secret  
3. PVC  
4. Déploiement MySQL  
5. Service MySQL  
6. Déploiement API  
7. Service API  
8. Ingress



_____________
pour vialiser les pods 
kubectl get pods -n jimpe    

http://4.251.145.205/jimpe/health
_____________




structure /kubernetes-brief
│
├── Namespace.yaml         # Déjà créé
├── Secret-MySQL.yaml      # Credentials MySQL (user/password)
├── PVC-MySQL.yaml         # PersistentVolumeClaim (disque Azure pour MySQL)
├── Deployment-MySQL.yaml  # Déploiement MySQL
├── Service-MySQL.yaml     # Service MySQL (ClusterIP)
├── Deployment-API.yaml    # Déploiement de l'API Python
├── Service-API.yaml       # Service API (ClusterIP)
└── Ingress.yaml           # Ingress vers l'API (endpoint /jimpe/…)






aide moi a faire ce projet 
merci



PS C:\Users\impej\Documents\BRIEF\Kubernetes> az login
A web browser has been opened at https://login.microsoftonline.com/organizations/oauth2/v2.0/authorize. Please continue the login in the web browser. If no web browser is available or if the web browser fails to open, use device code flow with `az login --use-device-code`.
The following tenants don't contain accessible subscriptions. Use 'az login --allow-no-subscriptions' to have tenant level access.
a2e466aa-4f86-4545-b5b8-97da7c8febf3 'Simplonformations.co'
[
  {
    "cloudName": "AzureCloud",
    "homeTenantId": "f6692c4e-06b5-4b04-b29e-2df99488ac33",
    "id": "029b3537-0f24-400b-b624-6058a145efe1",
    "isDefault": true,
    "managedByTenants": [
      {
        "tenantId": "2f4a9838-26b7-47ee-be60-ccc1fdec5953"
      }
    ],
    "name": "HDF_ROUBAIX_DATA_ENGINEER_P1_ALT_91373",
    "state": "Enabled",
    "tenantId": "f6692c4e-06b5-4b04-b29e-2df99488ac33",
    "user": {
      "name": "jimpe.ext@simplonformations.onmicrosoft.com",
      "type": "user"
    }
  }
]

[Survey] Help us improve Azure CLI by sharing your experience. This survey should take about 5 minutes. Run 'az survey' to open in browser. Learn more at https://go.microsoft.com/fwlink/?linkid=2203309

PS C:\Users\impej\Documents\BRIEF\Kubernetes> az aks get-credentials --resource-group RG_PROMO --name cluster_promo
Merged "cluster_promo" as current context in C:\Users\impej\.kube\config
PS C:\Users\impej\Documents\BRIEF\Kubernetes> 

